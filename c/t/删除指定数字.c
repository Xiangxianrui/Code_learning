#include <stdio.h>
void(char parr, int target, int sz)
{
    int slow = 0;
    int fast = 0;
    while (fast <)
}

int main()
{
    char arr[] = {0};
    int target = 0;

    printf("请输入一个整数字符串\n");
    scanf("%s", arr);
    printf("请输入要删除的数字(1-10):>");
    int sz = strlen(arr);

    remove(arr, target, sz);
    printf("arr\n");
}